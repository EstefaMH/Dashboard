let btn = document.getElementById("btn-logout");

btn.addEventListener((e)=>{
    e.preventDefault();

    handleLogout();
})


export const handleLogout = () => {
  
    let a
  
    return (
    <div>
        logout
    </div>
  )
}
