 function getAdmin() {
    
        const baseUrl = "http://localhost:3000";

        const headers = {
            method: "GET",
        }

        const response = await fetch(`${baseUrl}/admin`, headers);
        
   
    
}
